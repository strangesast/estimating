import { ObjToTypePipe } from './obj-to-type.pipe';

describe('ObjToTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjToTypePipe();
    expect(pipe).toBeTruthy();
  });
});
